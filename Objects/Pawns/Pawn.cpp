#include "Pawn.h"

void Pawn::Tick(const float DeltaSeconds)
{
    Actor::Tick(DeltaSeconds);

    //std::cout<<PawnCamera->GetObjectLocation().X<<" "<<PawnCamera->GetObjectLocation().Y<<std::endl;
    
}
